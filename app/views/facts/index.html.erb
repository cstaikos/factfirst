
  <%= "Showing results for \"#{params[:query]}\"" if params[:query] %>

  <%= render 'filter_nav' %>

  <div class="facts-container">
    <div id="facts-list" class="fact-box-wrapper">
      <% @facts.each do |fact| %>
        <%= render fact %>
      <% end %>
    </div>
  </div>
