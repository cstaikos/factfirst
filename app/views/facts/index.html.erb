
  <%= "Showing results for \"#{params[:query]}\"" if params[:query] %>

  <%= render 'filter_nav' %>

  <div class="facts-container">
    <div class="fact-box-wrapper">
      <div class="fact-boxes">
        <a href="javascript:void(0)" class="fact-box">
          <h1 class="fact-title"></i> Share Dynamic Images</h1>
          <p>Our dynamically generated fact infographics will change based on community feedback! Watch the state of your facts evolve.</p>
        </a>
      </div>

      <div class="fact-boxes">
        <a href="javascript:void(0)" class="fact-box">
          <h1 class="fact-title"></i> Share Dynamic Images</h1>
          <p>Our dynamically generated fact infographics will change based on community feedback! Watch the state of your facts evolve.</p>
        </a>
      </div>
      
      <div class="fact-boxes">
        <a href="javascript:void(0)" class="fact-box">
          <h1 class="fact-title"></i> Share Dynamic Images</h1>
          <p>Our dynamically generated fact infographics will change based on community feedback! Watch the state of your facts evolve.</p>
        </a>
      </div>
    </div>

    <ul id="facts-list">
      <% @facts.each do |fact| %>
        <%= render fact %>
      <% end %>
    </ul>
  </div>
