<div class="filter-buttons" data-selected="popular">
  <h4>Sort by:</h4>
  <div class="button-group">
    <label>
      <%= button_to 'Popular', {}, {
          :method => 'get',
          :name => 'sort',
          :remote => true,
          :url => facts_path + "?sort=popular"  } %>
      <span id="sort-popular" class="button-group-item<%= ' is-selected' if !params[:sort] || params[:sort].downcase == 'popular' %>">Popular</span>
    </label>

    <label>
      <%= button_to 'Debated', {}, {
          :method => 'get',
          :name => 'sort',
          :remote => true,
          :url => facts_path + "?sort=debated" } %>
      <span id="sort-debated" class="button-group-item<%= ' is-selected' if params[:sort] && params[:sort].downcase == 'debated' %>">Debated</span>
    </label>

    <label>
      <%= button_to 'New', {}, {
          :method => 'get',
          :name => 'sort',
          :remote => true,
          :url => facts_path + "?sort=new" } %>
      <span id="sort-new" class="button-group-item<%= ' is-selected' if params[:sort] && params[:sort].downcase == 'new' %>">New</span>
    </label>

    <label>
      <%= button_to 'Truth', {}, {
          :method => 'get',
          :name => 'sort',
          :remote => true,
          :url => facts_path + "?sort=truth" } %>
          <span id="sort-truth" class="button-group-item<%= 'is-selected' if params[:sort] && params[:sort].downcase == 'truth' %>">True</span>
    </label>

    <label>
      <%= button_to 'False', {}, {
          :method => 'get',
          :name => 'sort',
          :remote => true,
          :url => facts_path + "?sort=false" } %>
      <span id="sort-false" class="button-group-item<%= ' is-selected' if params[:sort] && params[:sort].downcase == 'false' %>">False</span>
    </label>

  </div>
</div>
